import { ElementIsInDirective } from './element-is-in.directive';

describe('ElementIsInDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementIsInDirective();
    expect(directive).toBeTruthy();
  });
});
